build:
  maxIssues: 0

config:
  validation: true

processors:
  active: true

style:
  MagicNumber:
    active: false
  WildcardImport:
    active: false

formatting:
  active: true
  android: true
  ImportOrdering:
    active: true
  NoWildcardImports:
    active: false
